<footer>
  <ul>
    {% for page in search.pages("notes") %}
    <li>
      <a href="{{ page.data.url }}">
        {{ page.data.title }}
      </a>
    </li>
    {% endfor %}
  </ul>

  <ul>  
    {% for link in links %}
    <li>
      <a href="{{ link.href }}" title="{{ link.title }}">
        {{ link.text }}
      </a>
    </li>
    {% endfor %}
  </ul>

  <p>
    <small>{{ languages[lang].copy | md(true) | safe }}</small>
  </p>
</footer>